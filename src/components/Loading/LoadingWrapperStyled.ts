import { Box } from 'reflexbox';
import styled from 'styled-components';

const LoadingWrapperStyled = styled(Box)({
  bottom: 0,
  left: 0,
  position: 'fixed',
  right: 0,
  top: 0,
});

export default LoadingWrapperStyled;
