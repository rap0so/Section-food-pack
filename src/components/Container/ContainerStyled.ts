import { Flex } from 'reflexbox';
import styled from 'styled-components';
import { border, compose } from 'styled-system';

const ContainerStyled = styled(Flex)(compose(border));

export default ContainerStyled;
